# 283. 移动零

## 目标

将所有 `0` 移到数组末尾，同时保持非零元素的相对顺序，并且必须原地操作。

## 两种写法

### 覆盖再补零

先把所有非零元素依次写到数组前面，再从 `write` 开始填充 `0`。这种写法分支更少，也更容易证明正确。

### 双指针交换

用 `slow` 指向下一个非零元素的位置，扫描到非零值时与 `slow` 交换，再让 `slow` 前进。

## 复习问题

为什么覆盖法能保持非零元素的相对顺序？交换法在数组几乎没有零时，是否可以减少一次自交换？
